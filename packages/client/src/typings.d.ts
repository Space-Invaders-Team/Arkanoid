export type StringObject = {
  [key: string]: string;
};

type PropsWithChildren = {
  children: JSX.Element,
};

export type TThemes = {
  theme_id: number
  theme_name: string
  theme_icon: string
};
